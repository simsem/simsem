% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exportData.R
\name{exportData}
\alias{exportData}
\title{Export simulated datasets to external files}
\usage{
exportData(
  nRep,
  model,
  n,
  program = "Mplus",
  fileStem = "sim",
  miss = NULL,
  missCode = -999,
  datafun = NULL,
  pmMCAR = NULL,
  pmMAR = NULL,
  facDist = NULL,
  indDist = NULL,
  errorDist = NULL,
  sequential = FALSE,
  modelBoot = FALSE,
  realData = NULL,
  maxDraw = 50,
  misfitType = "f0",
  misfitBounds = NULL,
  averageNumMisspec = NULL,
  optMisfit = NULL,
  optDraws = 50,
  seed = 123321,
  silent = FALSE,
  multicore = FALSE,
  numProc = NULL,
  params = FALSE
)
}
\arguments{
\item{nRep}{Number of simulation replications.}

\item{model}{A \code{\linkS4class{SimSem}} object specifying the simulation model.}

\item{n}{Sample size for each replication.}

\item{program}{Character string specifying the target software.
Supported options include \code{"Mplus"} and \code{"LISREL"}.}

\item{fileStem}{Character string used as the prefix for exported filenames.}

\item{miss}{Optional missing-data mechanism specification passed to
\code{\link{sim}}.}

\item{missCode}{Numeric value used to represent missing values in the
exported datasets.}

\item{datafun}{Optional user-specified data generation function.}

\item{pmMCAR}{Proportion of missing completely at random.}

\item{pmMAR}{Proportion of missing at random.}

\item{facDist}{Distribution specification for latent variables.}

\item{indDist}{Distribution specification for indicators.}

\item{errorDist}{Distribution specification for residual errors.}

\item{sequential}{Logical indicating whether simulation should run
sequentially.}

\item{modelBoot}{Logical indicating whether model bootstrapping is used.}

\item{realData}{Optional dataset used for resampling.}

\item{maxDraw}{Maximum number of attempts for drawing valid parameter sets.}

\item{misfitType}{Type of population misfit measure used when drawing
parameters.}

\item{misfitBounds}{Optional bounds for acceptable population misfit.}

\item{averageNumMisspec}{Logical indicating whether misfit should be
averaged across misspecified parameters.}

\item{optMisfit}{Character specifying optimization of misspecification
draws (e.g., \code{"min"} or \code{"max"}).}

\item{optDraws}{Number of candidate draws used when optimizing
misspecification.}

\item{seed}{Random seed used for simulation.}

\item{silent}{Logical indicating whether progress messages are suppressed.}

\item{multicore}{Logical indicating whether parallel processing is used.}

\item{numProc}{Number of processors used when \code{multicore = TRUE}.}

\item{params}{Logical. If \code{TRUE}, the function also returns the
parameter values used in each simulation replication.}
}
\value{
If \code{params = TRUE}, returns the parameter values used in the
simulation. Otherwise, the function writes datasets to disk and returns
\code{NULL} invisibly.
}
\description{
Generates simulated datasets using \code{\link{sim}} and exports them to
data files that can be used by external SEM software such as Mplus or
LISREL.
}
\details{
The function first generates data using \code{\link{sim}} and then writes
the datasets to disk using filenames based on \code{fileStem}. Missing
values are replaced with a user-specified missing-data code before
exporting.

For Mplus, each replication is written as a separate \code{.dat} file,
and an additional file containing the list of dataset names is also
created. For LISREL, all datasets are stacked together and written to a
single file.


The function is intended for situations where simulated datasets must be
exported to external SEM programs rather than analyzed directly within R.

Missing values in generated datasets are replaced with the value specified
in \code{missCode} before exporting.

When \code{program = "Mplus"}, the function creates:
\itemize{
\item One data file per replication (e.g., \code{sim1.dat}, \code{sim2.dat})
\item A file containing the list of dataset names
}

When \code{program = "LISREL"}, all datasets are stacked and written to a
single data file.
}
\examples{
\dontrun{
exportData(
  nRep = 10,
  model = myModel,
  n = 500,
  program = "Mplus",
  fileStem = "simdata"
)
}

}
\seealso{
\code{\link{sim}}, \code{\link{draw}}
}

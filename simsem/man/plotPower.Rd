% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPowerCoverage.R
\name{plotPower}
\alias{plotPower}
\title{Plot statistical power curves for model parameters}
\usage{
plotPower(
  object,
  powerParam,
  alpha = 0.05,
  contParam = NULL,
  contN = TRUE,
  contMCAR = TRUE,
  contMAR = TRUE,
  useContour = TRUE
)
}
\arguments{
\item{object}{A \code{\linkS4class{SimResult}} object that includes at least
one randomly varying parameter (e.g., sample size, percent missing,
or model parameters).}

\item{powerParam}{Vector of parameter names for which power should be
plotted (e.g., `"f1=~y2"` or `"f1~~f2"`).}

\item{alpha}{Significance level used to determine statistical significance.}

\item{contParam}{Vector of parameter names that vary across replications
and should be used as predictors in the power plot.}

\item{contN}{Include varying sample size in the power plot if available.}

\item{contMCAR}{Include varying percentage of missing completely at random
(MCAR) in the power plot if available.}

\item{contMAR}{Include varying percentage of missing at random (MAR)
in the power plot if available.}

\item{useContour}{If two varying parameters are plotted, a contour plot is
used when \code{TRUE}. If \code{FALSE}, a perspective plot is used.}
}
\value{
No value is returned. This function produces plots.
}
\description{
Plot statistical power for one or more parameters as a function of
varying simulation conditions (e.g., sample size, percent missing,
or randomly varying model parameters).
}
\details{
Power is estimated by predicting whether each replication is
statistically significant using logistic regression and then
plotting the predicted probability of significance.


The function predicts whether each replication is statistically significant
using logistic regression without interaction terms, then plots the predicted
probability of significance across varying simulation conditions.
}
\examples{
\dontrun{
loading <- matrix(0, 6, 1)
loading[1:6, 1] <- NA
LY <- bind(loading, 0.4)
RPS <- binds(diag(1))
RTE <- binds(diag(6))

CFA.Model <- model(LY = LY, RPS = RPS, RTE = RTE, modelType = "CFA")

Output <- sim(NULL,
  n = seq(100, 200, 20),
  pmMCAR = c(0, 0.1, 0.2),
  model = CFA.Model
)

plotPower(Output, "f1=~y1", contMCAR = FALSE)
plotPower(Output, "f1=~y1")
}

}
\seealso{
\itemize{
\item \code{\linkS4class{SimResult}} for simulation results with varying parameters.
\item \code{\link{getPower}} to obtain numerical power estimates.
}
}
